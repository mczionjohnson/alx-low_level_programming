#include <stdio.h>

/**
 * main - prints the "Programming is like building a multilingual puzzle
 * return: returns zero if successful, non zero otherwise
 */
int main(void)
{
  puts("\"Programming is like building a multilingual puzzle");
  return (0);
}
