#include <stdio.h>

/**
 * main - calls the prinf function
 * return: zero if successful, non zero otherwise 
 * */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n)";
	return (0);
}
